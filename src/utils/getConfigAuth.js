import axios from "axios"

const getConfigAuth = () => {

    
    return {
        const config = {
            header: {
                Authorization: `bearer ${'hola'}`
            }
        }
    }
}

export default getConfigAuth


